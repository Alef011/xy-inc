module.exports = {
  up: (queryInterface, Sequelize) =>
    queryInterface.createTable('pois', {
      id: {
        type: Sequelize.INTEGER,
        autoIncrement: true,
        allowNull: false,
        primaryKey: true,
      },
      name: {
        type: Sequelize.STRING,
        allowNull: false,
      },
      coordinate_x: {
        type: Sequelize.INTEGER,
        allowNull: false,
      },
      coordinate_y: {
        type: Sequelize.INTEGER,
        allowNull: false,
      },
      created_at: {
        type: Sequelize.DATE,
        allowNull: false,
      },
      updated_at: {
        type: Sequelize.DATE,
        allowNull: false,
      },
    }),

  down: queryInterface => queryInterface.dropTable('pois'),
};
